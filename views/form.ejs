<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/output.css" />
    <title>Mini Message Board - New Message</title>
  </head>
  <body class="h-screen p-4 bg-white dark:bg-gray-900">
    <main class="w-full mx-auto md:w-5/6 lg:w-1/2">
      <h1 class="font-bold text-3xl my-8 text-black dark:text-white">
        New Message
      </h1>

      <form
        class="flex items-center flex-col gap-4 py-6 px-4 bg-slate-50 dark:bg-gray-800 rounded"
        method="POST"
        action="/new"
      >
        <label
          class="gap-2 flex flex-col md:w-1/2 w-full text-black dark:text-gray-200"
          for="user"
        >
          Name:
          <input
            class="border-2 border-gray-500 dark:border-gray-100 rounded-md p-1"
            type="text"
            name="user"
            id="user"
            required
          />
        </label>
        <label
          class="gap-2 flex flex-col md:w-1/2 w-full text-black dark:text-gray-200"
          for="message"
        >
          Your Message:
          <textarea
            class="border-2 border-gray-500 dark:border-gray-100 rounded-md p-1 h-40"
            name="message"
            id="message"
            required
          ></textarea>
        </label>
        <button
          class="bg-green-600 px-8 py-2 text-white my-4 rounded"
          type="submit"
        >
          Send
        </button>
        <%- include("partials/error") %>
      </form>
    </main>
  </body>
</html>
